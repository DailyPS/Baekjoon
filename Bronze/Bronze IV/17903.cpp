#include <bits/stdc++.h>

using namespace std;

int main(void)
{
    int m, n;

    scanf("%d %d", &m, &n);

    if (m >= 8)
        printf("satisfactory");

    else
        printf("unsatisfactory");

    return 0;
}