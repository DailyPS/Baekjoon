#include <bits/stdc++.h>

using namespace std;

double a, b, c;

int main(void)
{
  cin.tie(NULL);
  ios::sync_with_stdio(false);

  cin >> a >> b >> c;

  cout << 0.229 * 0.324 * 2 * a + 0.297 * 0.42 * 2 * b + 0.21 * 0.297 * c;

  return 0;
}