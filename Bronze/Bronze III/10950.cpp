#include<bits/stdc++.h>

using namespace std;

int main(void)
{
    int num;
    cin >> num;
    for(int i = 0; i < num; i++)
    {
        int a, b;
        cin >> a >> b;
        cout << a + b << endl;
    }
    
    return 0;
}