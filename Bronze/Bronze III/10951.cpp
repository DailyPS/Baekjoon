#include<bits/stdc++.h>

using namespace std;

int main(void)
{
    while(true)
    {
        int a, b;
        cin >> a >> b;
        if (cin.eof() == true)
            break;
        else
            cout << a + b << endl;
    }
    
    return 0;
}